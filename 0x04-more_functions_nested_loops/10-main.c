#include "main.h"

/**
 * main - check code
 *
 * Return: 0 always
 */

int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	print_triangle(0);

	return (0);
}
